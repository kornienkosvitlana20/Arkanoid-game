# Діаграма діяльності (Activity Diagram)

```
[Старт]
   │
   ▼
Ініціалізація (Settings, pygame)
   │
   ▼
Показати стартовий екран
   │
   ▼
Чекати SPACE ──────────────────────────────┐
   │                                        │
   ▼                                        │
Запустити м'яч                             │
   │                                        │
   ▼                                        │
┌─ Ігровий цикл ─────────────────┐         │
│                                 │         │
│  Опрацювати події               │         │
│     │                           │         │
│     ├── ESC ──────────────────► ВИХІД    │
│     ├── P ──────────► Пауза              │
│     │                  │                  │
│     │             Чекати P  ◄────────────┘
│     │                                     │
│  Рух платформи                            │
│     │                                     │
│  Оновити м'яч                             │
│     │                                     │
│  Перевірити колізії                       │
│     ├── Цеглина? ──► +Score, знищити     │
│     ├── Платформа? ─► відбиття            │
│     └── Стіна? ────► відбиття            │
│                                           │
│  М'яч впав?                              │
│     ├── Так: lives -= 1                  │
│     │       ├── lives == 0? ──► Game Over│
│     │       └── інакше ──────► Respawn  │
│     └── Ні: продовжити                  │
│                                          │
│  Всі цеглини? ─── Так ──► Level Clear  │
│     │                         │          │
│     │                    Останній рівень?│
│     │                    ├── Так ──► WIN│
│     │                    └── Ні ──► +1  │
│     │                                   │
│  Малювати кадр                          │
└─────────────────────────────────────────┘
```

---

# Діаграма класів (Class Diagram)

```
┌─────────────────────┐
│      Settings       │
├─────────────────────┤
│ + SCREEN_WIDTH: int │
│ + SCREEN_HEIGHT: int│
│ + ball_speed: float │
│ + paddle_speed: int │
│ + bg_color: tuple   │
│ + lives: int        │
│ + difficulty: str   │
└─────────────────────┘
          │
          │ uses
          ▼
┌─────────────────────┐        ┌──────────────────┐
│        Game         │───────►│       UI         │
├─────────────────────┤ uses   ├──────────────────┤
│ - state: GameState  │        │ - font_large     │
│ - score: int        │        │ - font_medium    │
│ - lives: int        │        ├──────────────────┤
│ - level: int        │        │ + draw_hud()     │
├─────────────────────┤        │ + draw_start()   │
│ + run()             │        │ + draw_pause()   │
│ + _update()         │        │ + draw_game_over │
│ + _draw()           │        │ + draw_win()     │
│ + _handle_events()  │        └──────────────────┘
└──────┬──────────────┘
       │ contains
       ├──────────────────────────────────┐
       │              │                   │
       ▼              ▼                   ▼
┌────────────┐  ┌──────────────┐  ┌─────────────────┐
│    Ball    │  │    Paddle    │  │    BrickGrid    │
├────────────┤  ├──────────────┤  ├─────────────────┤
│ - x: float │  │ - x: float   │  │ - bricks: list  │
│ - y: float │  │ - y: float   │  ├─────────────────┤
│ - vx: float│  │ - width: int │  │ + reset()       │
│ - vy: float│  │ - speed: int │  │ + all_cleared() │
│ - active   │  ├──────────────┤  │ + draw()        │
├────────────┤  │ + move_left()│  └────────┬────────┘
│ + reset()  │  │ + move_right │           │ contains
│ + launch() │  │ + get_rect() │           ▼
│ + update() │  │ + draw()     │  ┌─────────────────┐
│ + draw()   │  └──────────────┘  │      Brick      │
│ + check_   │                    ├─────────────────┤
│  paddle_   │                    │ - x, y: float   │
│  collision │                    │ - hp: int       │
│ + check_   │                    │ - color: tuple  │
│  brick_    │                    │ - alive: bool   │
│  collision │                    ├─────────────────┤
└────────────┘                    │ + hit(): int    │
                                  │ + get_rect()    │
                                  │ + draw()        │
                                  └─────────────────┘
```
